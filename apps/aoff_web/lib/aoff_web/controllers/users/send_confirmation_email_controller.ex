defmodule AOFFWeb.Users.ConfirmationEmailController do
  use AOFFWeb, :controller

  def new(conn, params) do

    render(conn, "new.html")

  end

  def create(conn, params) do

  end

  def show(conn, params) do

  end
end